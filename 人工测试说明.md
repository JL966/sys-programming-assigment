# STC 学习板诊断使用说明

## 1. 烧录并打开

1. 两块板都烧录 `firmware\diagnostic\output\AcceptanceDiagnostic.hex`，运行时钟11.0592MHz。请使用仓库内的统一HEX。
2. 关闭STC-ISP和串口助手，双击 `启动学习板诊断.cmd`。已开网页按 Ctrl+F5 刷新。
3. 连接被测板：数码管应显示 `01`。连接辅助板：应显示 `02`。尚未连接PC时显示 `00`。
4. 点击“交换两板”，确认01和02也交换；再换回来。两板均接同一电脑USB，红外无需连接485线。

## 2. 各项操作与反馈

在下拉框选择项目，再点“单项测试”。按页面短步骤操作；准备时不计时，开始动作后出现剩余秒数。

| 项目 | 你要做什么 | 应看到什么 |
|---|---|---|
| 05 按键 | 依次按下并松开K1、K2、K3 | 各步从0变1/2/3；页面显示已按下、已松开 |
| 06 导航 | 依次上、下、左、右、中 | 各步从0变1/2/3/4/5；每步15秒 |
| 08 温度 | 采基线；再次开始后轻捂Rt | 数码管整数摄氏度，页面显示ADC、基线、变化量；30秒内响应 |
| 09 光照 | 采基线；开始后遮挡，再按提示开始后移开 | 数码管0～1023 ADC变化并恢复；两个动作各20秒 |
| 10 霍尔 | 起初移开磁铁，开始后靠近再移开 | 0→1→2；靠近、移开各15秒；无反应可换磁铁方向 |
| 11 振动 | 开始后轻晃 | 0变1或更大（事件次数，不是振动强度） |
| 02 串口 | 无需操作 | 网页挑战1～10，板上数字进度；随后坏帧/半帧检查 |
| 12 RTC | 无需操作 | 板上HH-MM-SS走动，页面三次时间读数 |
| 13 EEPROM | 不要拔电、复位 | 板上1～5阶段，页面备份/写入/校验/恢复/最终校验 |
| 15 红外 | 两板红外相对、10～20厘米无遮挡 | 网页双向各5包，允许重试；每方向至少4包才正常 |

红外板上左侧1表示“被测→辅助”，2表示反向；最右位是**本板在该方向接收到的不同有效包数**。发送板在该方向显示0是正常现象。网页显示两端汇总。

温度整数可能暂时不变但ADC已微变：并不矛盾，判定用ADC，不要求凑足1℃。超出标定表时板上显示横杠，不伪装为正常温度。RTC只检查通电走时，**没有验证后备电池断电保时**。EEPROM只抽测0x7F，不代表全地址与长期掉电保持测试。

## 3. 验证异常、跳过和筛选

1. 按键测试点击开始后故意不按：约15秒后红色异常；这验证“无响应”分支，不证明模拟了真实焊接故障。
2. 再单项复测并正确按键：应恢复绿色正常，旧记录仍在历史中。
3. 蜂鸣器等人工项目播放后不选择：30秒后橙色未测试；FM为60秒。
4. 红外用不透光物遮住测试，预期失败；移开再复测。不要拆件、短路或断电制造故障。
5. 没有485线：直接跳过16，橙色未测试。
6. 点击“只显示异常和未测试”：应留下所有红色与橙色项，绿色隐藏。
7. 任意项结束、跳过后，数码管恢复01/02。EEPROM取消会先恢复原值，稍等再操作，期间不要断电。

## 4. 整轮检查与记录

点击“一键整轮”，完成01～15，跳过16。全部已测正常且485未测时，总状态应为橙色“未完成”，不是“本轮通过”。

导出HTML和JSON。若红外仍失败，请提供JSON、两板摆放照片、是否两板都重新烧录，以及交换被测/辅助角色后是否同样失败；新记录包含每次发送、接收长度、CRC、匹配与重试证据。

测试结论只适用于本次操作和环境。重要报告请导出保存，便于复查。
