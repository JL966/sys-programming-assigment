# 焊后无忧 · 人工测试说明（2.2.0）

## 先这样准备

1. 初次使用先断电拆下扩展器件；用STC-ISP烧录最新 `firmware/diagnostic/output/AcceptanceDiagnostic.hex`，运行时钟11.0592MHz。此后换器件不用重烧。
2. 关闭烧录软件和串口助手，双击“启动学习板诊断.cmd”，使用Edge或Chrome。
3. 点击“连接被测板”，选择对应串口。数码管待机显示01。只有15红外、16 RS485需要再连接辅助板，辅助板显示02。
4. 本轮建议先用“单项测试”测17～24，再做一次“一键整轮”回归1～24。

## 每次更换扩展器件

1. 页面暂停并保存后，拔掉被测板USB及其他电源，辅助板不用拔。
2. 点击“已拔掉USB”，拆下前一器件，按页面说明连接当前器件。
3. 重新插USB，点击“重新连接并继续”，选择原被测板的串口。
4. 确认刚插回的板显示01，再点击“身份正确”。
5. 准备好后点击“开始”，此时才启动倒计时。

同一USB插回后串口号可能变化，请按实际设备选择。没有永久板号，软件不能自动辨认是否拿错实物板，请自己核对。

## 新增项目怎样测

| 项目 | 操作 | 预期效果 |
|---|---|---|
| 17 SM步进电机 | 用原配SM插头接好；点击开始，观察全过程 | 正转60→120步/秒→停止→反转60→120→停止；转动各4秒、停止各3秒；最后人工确认 |
| 18 EXT双通道模拟输入 | 第一次P1.0接GND、P1.1接5V；通过后再次断电，交换两路接线 | 左四位P1.0、右四位P1.1；第一次低/高、第二次高/低，两次均通过才正常 |
| 19 超声波 | 先保持障碍物不动；提示后移近/移远至少5厘米 | 后四位厘米数随位置变化；前四位空白；无有效回波显示横线 |
| 20 直流电机 | 连接课程配套驱动组件；观察全过程 | 正转30%→70%→停止→反转30%→70%→停止；时间同17，最后人工确认 |
| 21 电子秤 | 空秤保持稳定；按提示放物体，再取下 | 后四位相对变化幅度增大，再回落；不是克数，超过9999显示9999 |
| 22 电子尺 | 保持初始位置；按提示拉出，再推回 | 后四位0～1023 ADC变化并恢复；不是毫米 |
| 23 电子转角测量器 | 开始后顺时针、逆时针各转几格 | 方向显示1/2，无新脉冲显示0；后四位累计计数，两个方向均有响应才通过 |
| 24 RFID | 先不放卡；就绪后按提示放卡，再移开 | 后四位0000→0001→0002→0003；分别为等待响应、就绪、UID稳定、移开 |

电机显示例：`1---0060`中的1是正转，0060是设定步速；`2---0070`中的2是反转，0070是设定PWM百分比。显示变化不等于电机真的转动，必须看实物。若异常，可在页面填写“反转不动”等备注。

EXT模拟输入低值应≤100、高值应≥900。这检查基本通路，不是精密5V电压检测。5V与GND绝不能短接。

RFID只支持课程配套适配组件和4字节UID卡；不写卡、不读保护块。不要把普通3.3V裸MFRC522直接接5V。无可靠实物图时页面不显示图片栏，请使用原配线束；不确定线序先跳过，不要试接。

## 原有功能回归

- 05/06：按页面要求按下并松开；数码管显示对应按键数字。
- 08/09：温度/光照先采基线，再按提示捂热或遮挡；板上显示温度或ADC。
- 10/11：磁铁靠近再离开显示0→1→2；轻晃后振动计数增加。
- 12：显示HH-MM-SS并走时，只验证通电走时，不验证后备电池。
- 13：自动备份、写入、校验、恢复，再校验；全程不要拔电或复位。
- 15：两板红外相对10～20厘米，双向各至少4/5个有效包。无需485线。
- 16：有线时断电接A-A、B-B和参考地；没线就跳过。两板USB接同一电脑。

其他声光项目按真实现象人工确认。

## 故意测试“失败”和“跳过”

1. 19开始变化检测后不移动障碍物：应超时异常，不得自动正常。
2. 22拉出但不推回：恢复阶段应超时异常。
3. 24不放卡：放卡阶段超时异常；不能直接通过。
4. 17/20运行结束后不确认：15秒后未测试；点击异常则红色异常。
5. 任一项点击跳过：橙黄色未测试。
6. 断电换件等待期间，不能出现动作超时。
7. 运行中意外断开USB：本项未测试，不应据此宣布某个器件坏了；电机输出依靠断电或固件通信失联保护停止。
8. 点击“只显示异常和未测试”：只保留红色与橙色项。

不要通过短接、拆焊或带电拔插制造故障。“故意不操作”只验证无响应分支，不等于复现所有真实焊接故障。

## 检查恢复与报告

1. 做完几项后，在换件准备阶段刷新页面。
2. 点击“恢复未完成轮次”，连接原被测板，再点恢复。
3. 原来已完成的项目应保留；当前未完成项目重新准备检测，不沿用断电前基线。
4. 导出HTML与JSON，检查项目状态、异常原因、备注和证据。
5. 记录保存在当前电脑、当前浏览器；清理浏览器数据会丢失，重要结果请导出。

有异常→发现异常；无异常但有未测试→未完成；24项全部正常或人工通过→本轮通过。

本次软件检查不能代替实物验收。如某项误判，请保存JSON，并注明器件型号、实际操作、板上显示和是否使用课程原配线束；收到反馈后再调整，不需要先提交GitHub。
